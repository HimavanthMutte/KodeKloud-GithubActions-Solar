name: Deploy - EC2
on: 
    push:
    workflow_dispatch:
jobs:
    deploying:
        runs-on: ubuntu-latest
        steps:
            -   name: Connecting to EC2
                uses: appleboy/ssh-action@v1
                with:
                    host: 13.203.205.202
                    username: himu
                    key: ${{ secrets.PRIVATE_KEY }}
                    script: |
                        docker images
                        docker ps
                        docker stop app || true
                        docker rm app || true
                        docker run -p 3000:3000 --name app docker.io/himu26/ci:3d3225608a62d42446e924ee1eae492f2cb4af44

